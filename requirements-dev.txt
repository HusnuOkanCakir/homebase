# Milestone 0 development tooling.
#
# Pinned exactly. `make check` and CI must run identical versions — a lint result
# that differs between a developer's machine and CI is a bug in this file, not a
# difference of opinion.
#
# Python-only by design: this repository's development machine has Node 12, which
# cannot run the JavaScript equivalents. Milestone 2 brings Node 20+ for the
# dashboard; until then, nothing here needs it.

# Documentation site
mkdocs==1.6.1
mkdocs-material==9.7.7

# Markdown linting
pymarkdownlnt==0.9.39

# YAML linting (workflows, issue templates, app manifests)
yamllint==1.38.0

# Contract validation
check-jsonschema==0.37.4
openapi-spec-validator==0.9.0

# Workflow security analysis: unpinned actions, injection-prone template
# expressions in run: blocks, over-broad permissions.
zizmor==1.29.0
