#!/usr/bin/env bash
set -euo pipefail

script_dir="$(CDPATH= cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
python3 -m unittest discover -v -s "${script_dir}/../lib" -p 'test_*.py'
exec python3 -m unittest discover -v -s "${script_dir}/../tests" -p 'test_*.py'
